<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config — Adaptive AI Engine
  - Production: HTTPS only, no cleartext
  - Debug: cleartext allowed only for localhost / emulator
  - Certificate pinning: configure your domain below for production
-->
<network-security-config>

    <!-- Production: HTTPS only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug: allow cleartext to local dev servers only -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Emulator loopback — cleartext OK in debug only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!--
    PRODUCTION: Uncomment and configure certificate pinning for your domain.
    Generate pins: openssl s_client -connect yourdomain.com:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | base64

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">yourdomain.com</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">YOUR_PRIMARY_PIN_BASE64=</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_BASE64=</pin>
        </pin-set>
    </domain-config>
    -->

</network-security-config>
